#include <iostream>
 
using namespace std;
 
int main() {
 
    long long int x, m;
    while(1)
    {
        cin>> x;
        cin>> m;
        
        if(x==0 && m==0) break;
        cout<<x*m<<endl;
    }
 
    return 0;
}
